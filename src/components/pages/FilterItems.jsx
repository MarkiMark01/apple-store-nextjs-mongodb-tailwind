import { Range } from "react-range";

import CircleIcon from "../icons/CircleIcon";

const FilterItems = ({
  handleInput,
  togglePriceSlider,
  isSliderVisible,
  input,
  priceRange,
  setPriceRange
}) => {
  return (
    <section>
      
    </section>
  );
};
export default FilterItems;
